The BitmapFont Example is simple J2ME MIDlet that shows the abilites of the bitmap fonts.

To build application just call "ant" from the command line.
You need to have the WTK_HOME environment variable points to an installed WTK.
